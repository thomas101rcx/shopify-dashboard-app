[project]
name = "shopify-dashboard-app"
version = "0.1.0"
description = "Streamlit app for Shopify sales data prep + account overview dashboard"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openpyxl>=3.1.5",
    "python-calamine>=0.7.0",
    "pyyaml>=6.0.3",
    "streamlit>=1.58.0",
]
